for concor in '0' '0.65';  do  
  for diffp in '0' '0.2' '0.5' '0.8'; do 
    for is_diff in '1'; do
      echo ${concor}; 
      con=${concor} diff=${diffp} is_diff=${is_diff} sbatch --job-name=sim_${concor}${diffp}${is_diff} modsimvars_diffusion.sh; 
      sleep 1; 
    done
  done; 
done